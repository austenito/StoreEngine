.flash
  = flash.notice

%table
  - @products.each do |product|
    %tr
      %td
        = product.name
      %td
        = product.description
      %td
        %form{action: '/add_item', method: 'post'}
          %input{type: "submit", value: "Buy!", id: product.id}
          %input{type: "hidden", value: product.id, name: "product_id"}
