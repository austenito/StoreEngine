.row-fluid
  .span3
    .row-fluid
      .span12
        %ul.breadcrumb
          %li Cart
    / /row
    .row-fluid.pull-left
      .span12.cart
        -@cart.products.each do |product|
          %ul
            %li
              .span8
                %span= image_tag product.image(:thumb)
              .span4
                %p= "Price $#{product.price}"
                = form_tag update_quantity_cart_path do 
                  = hidden_field_tag 'product_id', product.id
                  = text_field_tag 'quantity', @cart.cart_products.find_by_product_id(product.id).quantity
                  = submit_tag 'Update'
                %br Amount
                = @cart.cart_products.find_by_product_id(product.id).quantity * product.price
                = form_tag delete_product_cart_path do
                  = hidden_field_tag 'product_id', product.id
                  = submit_tag 'Remove from cart'
        %form{ action: checkout_path, method: 'get' }
        %input{ type: 'submit', name: 'checkout', value: 'checkout'}
    / /row

/ %table
/   %tr
/     %th Item
/     %th Quantity
/     %th Unit Price
/     %th Subtotal
/     -@cart.products.each do |product|
/       %tr
/         %td
/           = product.name
/         %td
/           = form_tag update_quantity_cart_path do 
/             = hidden_field_tag 'product_id', product.id
/             = text_field_tag 'quantity', @cart.cart_products.find_by_product_id(product.id).quantity
/             = submit_tag 'Update'
/         %td
/           = product.price
/         %td
/           = @cart.cart_products.find_by_product_id(product.id).quantity * product.price
/         %td
/           = form_tag delete_product_cart_path do
/             = hidden_field_tag 'product_id', product.id
/             = submit_tag 'Remove from cart'
/ %p

/ %form{ action: checkout_path, method: 'get' }
/   %input{ type: 'submit', name: 'checkout', value: 'checkout'}

